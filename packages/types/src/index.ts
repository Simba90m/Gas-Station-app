export * from "./roles";

// Domain types (Station, Service, Booking, Employee, etc.) will be added in
// Phase 2 once the Supabase database schema is finalized, and generated
// from it where possible so types and schema never drift apart.
